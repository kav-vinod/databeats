import 'package:flutter/material.dart';

class RecentSongPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Recent Songs')),
      body: Center(child: Text('Recent Songs')),
    );
  }
}